monkeytype:
  outputs:
    local:
      method: oauth
      dataset: monkeytype_stats
      priority: interactive
      project: "{{ env_var('GOOGLE_CLOUD_PROJECT_ID') }}"
      threads: 64
      type: bigquery

  target: local